# Gemini CLI Chatbot

A command-line chatbot using Google's Gemini Pro 2.0 Flash model.

## How to Run

1. Clone the repo:

2. Create a `.env` file and add your Gemini API key:

3. Install dependencies:

4. Run the chatbot:

## Deployment

This app is configured for Render deployment as a background worker.
